# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                                 controllers.Application.index
GET         /pdfGenerationDemo                controllers.Application.pdfGenerationDemo

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                     controllers.Assets.at(path="/public", file)


GET         /consent_to_publish               controllers.CopyrightTransfer.index
POST        /consent_to_publish               controllers.CopyrightTransfer.submit
POST        /consent_to_publish/doc           controllers.CopyrightTransfer.generateDoc

